{
  "name": "cli-weather-forecast",
  "version": "1.0.0",
  "description": "A CLI based program that outputs the weather forecast based on the users inputted location",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Benjamin Jackson",
  "license": "MIT",
  "dependencies": {
    "axios": "^1.5.1",
    "dotenv": "^16.3.1",
    "readline-sync": "^1.4.10"
  }
}
